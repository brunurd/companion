* {
  padding: 0;
  margin: 0;
  box-sizing: border-box;
}

body {
  overflow: hidden;
}

body, html {
  height: 100%;
  background-color: white;
}

a, a * {
  color: inherit;
  text-decoration: none;

  &:hover, &:hover * {
    text-decoration: none;
  }
}
